package TopicWise.LinkedList;

public class NodesPair {
    public Node head = null;
    public Node tail = null;

    public NodesPair(Node head, Node tail){
        this.head = head;
        this.tail = tail;
    }
}
